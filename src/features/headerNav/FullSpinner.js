import { Spinner } from "react-bootstrap"

const FullSpinner = () =>{
    return (
        
        <div className="full_spinner">
            <Spinner />
        </div>

    )
}
export default FullSpinner